DOCKER_IMAGE_NAME=hive
DOCKER_APP_NAME="${DOCKER_IMAGE_NAME}"

DOCKER_APP_INTERNAL_PORT=10000
DOCKER_APP_EXTERNAL_PORT="${DOCKER_APP_INTERNAL_PORT}"

DOCKER_DB_SHELL_COMMAND="/opt/hive/bin/hive"

# /opt/hive/bin/hive -hiveconf hive.root.logger=DEBUG,console

DOCKER_BUILD_ARGS="HIVE_VERSION=2.3.2"
