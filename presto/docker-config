DOCKER_IMAGE_NAME=presto
DOCKER_APP_NAME="${DOCKER_IMAGE_NAME}"

DOCKER_APP_INTERNAL_PORT=8080
DOCKER_APP_EXTERNAL_PORT="${DOCKER_APP_INTERNAL_PORT}"

DOCKER_DB_SHELL_COMMAND="/opt/presto/bin/prestoclient --catalog postgresql --schema test"

DOCKER_BUILD_ARGS="PRESTO_VERSION=0.189"
